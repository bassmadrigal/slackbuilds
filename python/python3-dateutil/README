python3-dateutil (extensions to the standard datetime module)

The dateutil module provides powerful extensions to the standard
datetime module.

Hompage: http://labix.org/python-dateutil

This builds the python3 version of dateutil. It is safe to install
this on a system that has SBo's python-dateutil (python2) package
installed.
